{
  "name": "riferrei.com",
  "private": true,
  "scripts": {
    "build": "hugo --gc --minify && npx --yes pagefind@^1 --site public",
    "serve": "hugo server",
    "index": "npx --yes pagefind@^1 --site public"
  },
  "devDependencies": {
    "pagefind": "^1.5.0"
  }
}
